import { describe, it } from "@jest/globals";

describe("exampleSlice", () => {
  it.todo("handles the initial state");
  it.todo("handles reducer logic for a known action");
});

describe("exampleAsyncThunk", () => {
  it.todo("dispatches fulfilled action on success");
  it.todo("dispatches rejected action on failure");
});
